/*Declaracion de Variables
var ya no es recomendable
let > Variable
const > Constante*/

let nombre //Algunas veces se puede declarar la variable sin datos dentro. 

nombre = "Jeniffer"

const cedula = "6-720-2244" //Constante
console.log(nombre , "Cedula: ", cedula);

/*Buenas practicas para variables
cammel
*/
let nombreCompleto = "Ricardo Dominguez"
console.log(nombreCompleto);

let x, y;
x = 10 , y = 30;
let z = x + y;
console.log(z);

/*Java scrip es sensible a mayusculas
# no pueden iniciar con numeros     mas si con _ $ luego del primer caracter ahi si se le puede asignar un numero. 

*/

